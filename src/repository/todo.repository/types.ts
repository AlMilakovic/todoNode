export type ToDo = {
  title: string;
  description: string;
  createdDate: string;
  id: string;
  userId: string;
};

export type User = {
  fullName: string;
  email: string;
  password: string;
};
